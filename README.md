# Pocket Calculator

A small calculator made with plain HTML, CSS, and JavaScript.

It handles addition, subtraction, multiplication, division, percentages, decimals, clearing, and deleting the last digit. The keyboard works too: press `Enter` for the result, `Escape` to clear, or `Backspace` to delete.

Open `index.html` in a browser to use it.